<% if flash[:not_found] %>
  <div class="alert alert-warning">
    <strong><%= flash[:not_found] %></strong>
  </div>
<% end %>
<%= form_for :client, url: clients_search_path do |f| %>
  <%= f.label :client_number %>:
  <%= f.text_field :client_number %><br />
  <%= f.submit "Buscar" %>
<% end %>
